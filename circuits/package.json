{
  "name": "@noir-rail/circuits",
  "version": "0.0.0",
  "private": true,
  "description": "NoirRail zero-knowledge circuits (Circom, BLS12-381) and trusted-setup ceremony",
  "scripts": {
    "build": "bash scripts/build.sh",
    "setup": "bash scripts/setup.sh withdraw",
    "all": "bash scripts/build.sh withdraw && bash scripts/setup.sh withdraw"
  }
}
